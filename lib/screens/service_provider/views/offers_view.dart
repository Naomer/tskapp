import 'package:flutter/material.dart';

class ServiceProviderOrdersView extends StatelessWidget {
  const ServiceProviderOrdersView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Service Provider Orders View'),
    );
  }
}
